using UnityEngine;

[CreateAssetMenu(menuName ="Space Pioneers/Set/Trajectory Set")]
public class TrajectoryRS : RuntimeSet<TrajectoryPreview>
{
    /*public bool block = false;

    public new void Add(TrajectoryPreview thing)
    {
        if(!block)
        {
            base.Add(thing);
        }
    }

    public new void Remove(TrajectoryPreview thing)
    {
        if(!block)
        {
            base.Remove(thing);
        }   
    }*/
}